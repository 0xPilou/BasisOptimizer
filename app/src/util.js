import detectEthereumProvider from '@metamask/detect-provider';
import { ethers } from 'ethers';


const getAccounts = () =>
  new Promise( async () => {
        const signer = provider.ge
    return;
  });

export default getAccounts;
